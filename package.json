{
  "name": "root",
  "scripts": {
    "start": "for p in . packages/project-eden-types packages/project-eden-ui; do $(cd $p && tmuxinator); done",
    "graduate": "npx lerna version --conventional-graduate"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
